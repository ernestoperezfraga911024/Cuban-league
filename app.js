async function load(){const d=await(await fetch('data.json')).json();const cur=[...d.participants].sort((a,b)=>b.points-a.points||a.id-b.id);season.textContent=d.season;updated.textContent=d.lastUpdated;leader.textContent=cur[0]?.name||'—';standings.innerHTML=cur.map((p,i)=>`<div class="row current-grid"><span class="position">${i+1}</span><div class="team"><img src="${p.shield}"><span class="team-name">${p.name}</span></div><span class="played">${p.played}</span><span class="points">${p.points}</span></div>`).join('');const img=Object.fromEntries(d.participants.map(p=>[p.name,p.shield]));const h=[...d.historical].sort((a,b)=>b.titles-a.titles||b.podiums-a.podiums||b.top5-a.top5||b.points-a.points);history.innerHTML=h.map((p,i)=>`<div class="hist-row"><span class="position">${i+1}</span><div class="team"><img src="${img[p.name]||''}"><span class="team-name">${p.name}</span></div><span class="center">${p.titles}</span><span class="center">${p.podiums}</span><span class="center">${p.seasons}</span><span class="num">${p.points.toLocaleString()}</span><span class="num">${p.avg?p.avg.toLocaleString(undefined,{maximumFractionDigits:1}):'—'}</span></div>`).join('')}document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');const h=b.dataset.view==='history';currentView.hidden=h;historyView.hidden=!h});shareButton.onclick=async()=>navigator.share?navigator.share({title:'Cuban League',url:location.href}):navigator.clipboard.writeText(location.href);load();